#include "clienttcp.h"

int main()
{
	//declares for ease of use
	clienttcp client;

	//constants
	bool running = false;

		//running functions
		client.initNet();
		while (running = true)
		{

		}
}